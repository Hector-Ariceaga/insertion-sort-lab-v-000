function findMinAndRemove(array){
  let minimum = array[0]
  let minIndex = 0
  for (let i=0; i < array.length; i++) {
    if (i < minimum) {
      minimum = array[i]
    }
  return minimum
  }
}

function insertionSort(array){

}
